pipeline {
    agent any

    stages {
        stage('Starting Pipeline!') {
            steps {
                echo 'Starting Pipeline Again as new changes are found!!!!!!!'
            }
        }
    }
}
